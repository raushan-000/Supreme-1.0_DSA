#include <iostream>

int main()
{
    /*
    // conditional statement

    // declare integer type variable

    int score;
    // print Enter the score
    std::cout << "Enter score" << std::endl;
    // take input into score variable
    std::cin >> score;

    // condition
    if (score > 300)
    {
        std::cout << "India wins" << std::endl;
    }
    else
    {

        std::cout << "Australia wins" << std::endl;
    }

    // grade according to marks

    int marks;
    std::cout << "Enter marks between 1 to 100" << std::endl;
    std::cin >> marks;
    if (marks < 0 || marks > 100)
    {
        std::cout << "Invalid marks" << std::endl;
    }
    else if (marks > 90)
    {
        std::cout << "A" << std::endl;
    }
    else if (marks >= 80)
    {
        std::cout << "B" << std::endl;
    }
    else if (marks >= 60)
    {
        std::cout << "C" << std::endl;
    }
    else if (marks >= 40)
    {
        std::cout << "D" << std::endl;
    }
    else
    {
        std::cout << "F" << std::endl;
    }

    */

    // Loop

    //    for(int i=0; i<5; i=i+1){
    //     std::cout<<i;
    //    }
    //    for(int i=5; i>0; i=i-1){
    //     std::cout<<i;
    //    }

    for (int i = 0; i <= 5; i = i + 2)
    {
        std::cout << i;
    }
}
